import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Home = () => {
    const topics_raw = useLoaderData();
    return (
        <div>
            
        </div>
    );
};

export default Home;
import React from 'react';
import './Options.css'
const Options = ({ option }) => {
    return (
        <div className='option'>
            {option}
        </div>
    );
};

export default Options;